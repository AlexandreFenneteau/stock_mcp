[project]
name = "agent-client"
version = "0.1.0"
description = "Local fast-agent client for the Stock Management MCP Server demo"
requires-python = ">=3.12,<3.15"
dependencies = [
    "fast-agent-mcp>=0.9.0",
]
